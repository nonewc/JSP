package com.ok;

public class classmemo {

/*
	## PreparedStatement 객체의 사용
	- Statement객체와 PreparedStatement 객체는 쿼리문을 실행하는 동일한 기능 제공
	- PreparedStatemnet객체를 사용하는 이유는 객체가 값 변환을 자동으로 해주는 기능을 제공하고
		코드를 간결하게 만들 수 있기 때문
	- Statement객체는 지정할 값이 많아질 경우 따옴표가 복잡하게 얽히기 때문에,
		코드 작성시 오류가 발생할 확률이 높고, 수정하기에도 어렵다.
	- PreparedStatement 객체는 이런 문제를 쉽게 해결함. 
		값을 지정하는 부분에 "?"로 처리하기 때문에 간단히 값을 지정하고,
		"?" 는 1부터 시작하는 인덱스 값을 가지고, 1씩 증가하며 처리됨.
	
 
 
 
 
 
 
*/
	
	
	
}
